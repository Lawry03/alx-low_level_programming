#include <stdio.h>
#include "main.h"
/**
 * add - function adds two integers.
 * @x: first Integer.
 * @y: second Integer.
 * Return: The sum of the integers (x + y).
 */
int add(int x, int y)
{
	return (x + y);
}
