#include <stdio.h>
#include "main.h"
/**
 * add - function adds two integers.
 * @x: first Integer
 * @y: second Integer
 * Returns: The sum of the integers.
 */
int add(int x, int y)
{
	return (x + y);
}
